type DiffType = 'added' | 'changed' | 'removed';
namespace DiffType {
    export const ADDED: DiffType = 'added';
    export const CHANGED: DiffType = 'changed';
    export const REMOVED: DiffType = 'removed';
}

export default DiffType;
